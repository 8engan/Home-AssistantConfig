alias: "frida lämnar hem - bool"
trigger:
  platform: template
  value_template: "{% if (not is_state('device_tracker.frida_ios','home')) and (not is_state('device_tracker.frida_ping','home')) and (not is_state('device_tracker.frida_frida_owntracks','home')) %}true{% endif %}"
  #value_template: "{% if (is_state('device_tracker.frida_bengtphone','home')) %}true{% endif %}"
condition:
  condition: state
  entity_id: input_boolean.frida_hemma
  state: 'on'
action:
  service: input_boolean.turn_off
  entity_id: input_boolean.frida_hemma